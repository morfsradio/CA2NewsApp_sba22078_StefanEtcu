import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ca2mobdev_sba22078',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
